# backened
Successfully built a simple Notes API using Node.js, Express, and MongoDB. Implemented full CRUD operations (Create, Read, Update, Delete) and connected the backend to MongoDB Atlas using Mongoose. Learned how data flows from client requests to database storage and how APIs handle real-time operations.
